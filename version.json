{
  "sourceOfTruth": "version.js",
  "purpose": "Runtime/build version is defined only in version.js. This file intentionally does not carry a version flag so the app has one authoritative version source.",
  "notes": "Install the matching version.js and index.html from the package. index.html loads version.js with a no-store probe and uses those runtime variables for app/data cache busting."
}
